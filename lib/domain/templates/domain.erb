require_relative 'support/string'
require_relative 'helpers/command_loader'
require 'singleton'

<% @aggregates.each do |aggregate| %>
require_relative '<%= folder_name %>/<%= aggregate.folder_name %>'
<% end %>

module <%= @name %>
end